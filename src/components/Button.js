const Button = ({ text })  => {
    return (
        <button className="btn bg-gradient-to-r from-doctor-green to-doctor-blue text-neutral-50 font-bold ">{text}</button>
    )
}

export default Button